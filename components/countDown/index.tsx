import * as React from "react";

class CountDown extends React.Component {
    public render() {
        const module = (
            <span>倒计时321</span>
        );
        return module;
    }
}

export default CountDown;
